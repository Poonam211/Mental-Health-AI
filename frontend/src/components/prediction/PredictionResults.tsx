import React, { useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { 
  BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend 
} from 'recharts';
import { 
  FiDownload, FiShare2, FiHeart, FiAward, 
  FiCheckCircle, FiCopy, FiMail, FiTwitter 
} from 'react-icons/fi';

interface PredictionResultsProps {
  result: any;
  onReset: () => void;
  age?: number;
  city?: string;
  gender?: string;
  occupation?: string;
  sleepHours?: number;
}

const PredictionResults: React.FC<PredictionResultsProps> = ({ 
  result, onReset, age = 25, city = 'Pune', gender = 'Male', occupation = 'Employee', sleepHours = 7.0 
}) => {
  const [shareOpen, setShareOpen] = useState<boolean>(false);
  const [toastMessage, setToastMessage] = useState<string | null>(null);

  if (!result) return null;

  // Calculate needle coordinates for the semi-circular SVG Risk Gauge
  const angleDeg = 180 - (result.risk_score / 100) * 180;
  const angleRad = (Math.PI * angleDeg) / 180;
  const needleRadius = 60;
  const cx = 100;
  const cy = 100;
  const targetX = cx + needleRadius * Math.cos(angleRad);
  const targetY = cy - needleRadius * Math.sin(angleRad);

  // Data for Recharts comparison
  const comparisonData = [
    { name: 'Depression', User: result.depression_percent, Average: 38.5 },
    { name: 'Anxiety', User: result.anxiety_percent, Average: 34.2 },
    { name: 'Overall Stress', User: result.stress_percent, Average: 42.18 }
  ];

  // Document download logic
  const handleDownload = () => {
    const reportText = `MENTAL HEALTH RISK ASSESSMENT REPORT
=====================================
Generated At: ${new Date().toLocaleString()}
Status: Anonymized Clinical Intake

DEMOGRAPHIC METRICS:
--------------------
Age: ${age}
Gender: ${gender}
City: ${city}
Occupation: ${occupation}
Sleep Cycle: ${sleepHours} hours/day

CLINICAL EVALUATION INDICATORS:
-------------------------------
Depression Score (PHQ-9): ${result.depression_score} / 27 (${result.depression_percent.toFixed(2)}% Intensity)
Anxiety Score (GAD-7): ${result.anxiety_score} / 21 (${result.anxiety_percent.toFixed(2)}% Intensity)
Estimated Overall Stress: ${result.stress_percent.toFixed(2)}%
Overall Mental Wellness Index: ${result.wellness_score.toFixed(2)} / 100

CLINICAL CLASSIFICATION:
------------------------
Calculated Risk Level: ${result.risk_level}
Diagnostic Mental State: ${result.mental_state}

AI NATURAL LANGUAGE SYMPTOM ANALYSIS:
-------------------------------------
Anxiety Indicators Found: ${result.symptom_analysis.Anxiety}
Depression Indicators Found: ${result.symptom_analysis.Depression}
Stress Indicators Found: ${result.symptom_analysis.Stress}

CLINICAL RECOMMENDATIONS & REMEDIES:
------------------------------------
${result.recommendations.map((rec: string, idx: number) => `${idx + 1}. [✔] ${rec}`).join('\n')}

=====================================
DISCLAIMER: This report is generated by an AI prediction model for observatory and research purposes. It does NOT constitute a formal psychiatric diagnosis or medical prescription. Always consult a licensed therapist or clinician for professional health treatments.
`;

    const blob = new Blob([reportText], { type: 'text/plain' });
    const url = window.URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.setAttribute('href', url);
    a.setAttribute('download', `MentalHealth_AI_Report_${city.replace(/\s+/g, '_')}.txt`);
    a.click();
  };

  const handleCopyLink = () => {
    navigator.clipboard.writeText(`http://127.0.0.1:3000/share/report_${Math.random().toString(36).substring(2, 11)}`);
    showToast("Anonymized report link copied to clipboard!");
  };

  const showToast = (msg: string) => {
    setToastMessage(msg);
    setTimeout(() => {
      setToastMessage(null);
    }, 2500);
  };

  // Custom Glassmorphic Dark Tooltip for Recharts
  const CustomTooltip = ({ active, payload, label }: any) => {
    if (active && payload && payload.length) {
      return (
        <div className="bg-slate-900/95 dark:bg-slate-950/95 border border-slate-800 dark:border-slate-800/80 p-3.5 rounded-2xl shadow-xl text-xs text-white backdrop-blur-md">
          <p className="font-black mb-1.5 uppercase tracking-wider text-[10px] text-slate-400">{label}</p>
          {payload.map((p: any, idx: number) => (
            <p key={idx} className="font-bold flex justify-between space-x-6 py-0.5">
              <span style={{ color: p.color || p.fill }}>{p.name}:</span>
              <span>{p.value.toFixed(1)}%</span>
            </p>
          ))}
        </div>
      );
    }
    return null;
  };

  return (
    <motion.div
      initial={{ opacity: 0, y: 15 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.45 }}
      className="space-y-8 text-left font-medium relative"
    >
      {/* Top Header Controls */}
      <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 border-b border-slate-200 dark:border-slate-750 pb-4.5">
        <div>
          <h3 className="text-xl font-black text-slate-900 dark:text-white leading-tight tracking-tight">
            Clinical Evaluation Results
          </h3>
          <p className="text-[10px] text-slate-400 dark:text-slate-500 font-extrabold uppercase tracking-wider mt-1">
            AI-Powered Personal Risk Summary
          </p>
        </div>
        <div className="flex space-x-2.5">
          <button
            onClick={handleDownload}
            className="flex items-center space-x-2 rounded-xl border border-slate-200 dark:border-slate-700 text-slate-600 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800 hover:text-slate-950 dark:hover:text-white px-4 py-2.5 font-bold text-xs transition-all duration-200"
          >
            <FiDownload className="w-4 h-4" />
            <span>Download Report</span>
          </button>
          <button
            onClick={() => setShareOpen(true)}
            className="flex items-center space-x-2 rounded-xl border border-slate-200 dark:border-slate-700 text-slate-600 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800 hover:text-slate-950 dark:hover:text-white px-4 py-2.5 font-bold text-xs transition-all duration-200"
          >
            <FiShare2 className="w-4 h-4" />
            <span>Share Link</span>
          </button>
        </div>
      </div>

      {/* Section 1: Risk Gauge & Mental Wellness Card */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        
        {/* Risk Gauge Card */}
        <div className="bg-white dark:bg-slate-850/80 p-6 rounded-3xl border border-slate-200/85 dark:border-slate-750/70 flex flex-col items-center justify-center text-center shadow-md shadow-slate-100/30 dark:shadow-none backdrop-blur-md">
          <h4 className="text-sm font-black text-slate-800 dark:text-slate-200 mb-4 uppercase tracking-wider">
            🎯 Stress Risk Speedometer
          </h4>
          <div className="w-56 h-36 relative">
            <svg className="w-full h-full" viewBox="0 0 200 120">
              {/* Background Arc */}
              <path
                d="M 20 100 A 80 80 0 0 1 180 100"
                fill="none"
                stroke="#f1f5f9"
                className="dark:stroke-slate-800"
                strokeWidth="14"
                strokeLinecap="round"
              />
              {/* Colored Risk Zones (Gradient Arc) */}
              <path
                d="M 20 100 A 80 80 0 0 1 180 100"
                fill="none"
                stroke="url(#risk-gradient)"
                strokeWidth="14"
                strokeLinecap="round"
                strokeDasharray="251.2"
                strokeDashoffset="0"
              />
              {/* Gradient Definition */}
              <defs>
                <linearGradient id="risk-gradient" x1="0%" y1="0%" x2="100%" y2="0%">
                  <stop offset="0%" stopColor="#10b981" /> {/* Emerald */}
                  <stop offset="50%" stopColor="#f59e0b" /> {/* Amber */}
                  <stop offset="100%" stopColor="#ef4444" /> {/* Red */}
                </linearGradient>
              </defs>
              
              {/* Central Cap */}
              <circle cx={cx} cy={cy} r="10" fill="#6366f1" className="shadow-lg" />
              <circle cx={cx} cy={cy} r="5" fill="#ffffff" />
              {/* Needle/Pointer */}
              <line
                x1={cx}
                y1={cy}
                x2={targetX}
                y2={targetY}
                stroke="#6366f1"
                strokeWidth="4.5"
                strokeLinecap="round"
              />
            </svg>
            {/* Text Badge */}
            <div className="absolute bottom-3 left-1/2 -translate-x-1/2 text-center">
              <h4 className="text-3xl font-black text-slate-900 dark:text-white leading-none">
                {result.risk_score.toFixed(1)}%
              </h4>
              <p className="text-[9px] font-black text-slate-400 dark:text-slate-500 uppercase tracking-widest mt-1">
                Calculated Stress Risk
              </p>
            </div>
          </div>
        </div>

        {/* Mental Wellness Card */}
        <div className="bg-gradient-to-br from-indigo-50/30 to-indigo-100/5 dark:from-indigo-950/20 dark:to-slate-850/40 p-6 rounded-3xl border border-indigo-100/60 dark:border-slate-750/85 flex flex-col justify-between h-full shadow-md shadow-slate-100/10 dark:shadow-none backdrop-blur-md">
          <div className="space-y-3.5">
            <div className="flex justify-between items-start">
              <div className="space-y-1">
                <p className="text-[10px] font-black text-indigo-600 dark:text-indigo-400 uppercase tracking-wider">
                  Wellness Quotient
                </p>
                <h3 className="text-4xl font-black text-slate-900 dark:text-white leading-none">
                  {result.wellness_score.toFixed(1)}<span className="text-base text-slate-400 font-bold">/100</span>
                </h3>
              </div>
              <div className="bg-indigo-600/10 dark:bg-indigo-400/10 p-2.5 rounded-xl border border-indigo-100/20 text-indigo-600 dark:text-indigo-400 flex items-center justify-center">
                <FiAward className="w-6 h-6" />
              </div>
            </div>
            <p className="text-xs text-slate-500 dark:text-slate-400 leading-relaxed font-medium">
              {result.wellness_score >= 65 
                ? 'Your wellness index indicates healthy and stable cognitive pathways. Focus on preventative practices to maintain this resilience.'
                : result.wellness_score >= 40
                  ? 'Your wellness index reflects moderate emotional strain. Implementing structured routines and mindful breathwork is highly recommended.'
                  : 'Your wellness index indicates significant stress intensities. Active self-care and consulting a professional therapist is advised.'}
            </p>
          </div>
          <div className="pt-4 border-t border-indigo-100/30 dark:border-slate-750/60 grid grid-cols-2 gap-4 mt-4">
            <div className="space-y-1">
              <p className="text-[9px] font-black text-slate-400 dark:text-slate-500 uppercase tracking-widest">
                Risk Rating
              </p>
              <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider ${
                result.risk_level === 'High' ? 'bg-red-500/10 text-red-600 border border-red-200/40 dark:border-red-900/40' :
                result.risk_level === 'Medium' ? 'bg-amber-500/10 text-amber-600 border border-amber-200/40 dark:border-amber-900/40' :
                'bg-emerald-500/10 text-emerald-600 border border-emerald-200/40 dark:border-emerald-900/40'
              }`}>
                {result.risk_level}
              </span>
            </div>
            <div className="space-y-1">
              <p className="text-[9px] font-black text-slate-400 dark:text-slate-500 uppercase tracking-widest">
                Diagnostic State
              </p>
              <p className="text-xs font-bold text-slate-850 dark:text-slate-200 truncate" title={result.mental_state}>
                {result.mental_state}
              </p>
            </div>
          </div>
        </div>

      </div>

      {/* Section 2: Progress Rings (Depression, Anxiety, Stress) */}
      <div className="grid grid-cols-1 sm:grid-cols-3 gap-5">
        {[
          { title: 'Depression (PHQ-9)', score: result.depression_score, max: 27, percent: result.depression_percent, color: 'stroke-indigo-600' },
          { title: 'Anxiety (GAD-7)', score: result.anxiety_score, max: 21, percent: result.anxiety_percent, color: 'stroke-amber-500' },
          { title: 'Stress (ML Risk)', score: Number(result.risk_score).toFixed(0), max: 100, percent: result.stress_percent, color: 'stroke-red-500' },
        ].map((ring, idx) => (
          <div 
            key={idx} 
            className="bg-white dark:bg-slate-850/80 p-5 rounded-3xl border border-slate-200/80 dark:border-slate-750/70 flex flex-col items-center justify-center text-center shadow-md shadow-slate-100/20 dark:shadow-none backdrop-blur-md"
          >
            <p className="text-[9px] font-black text-slate-400 dark:text-slate-400 uppercase tracking-wider mb-4">
              {ring.title}
            </p>
            <div className="w-24 h-24 relative flex items-center justify-center">
              <svg className="w-full h-full transform -rotate-90">
                <circle cx="48" cy="48" r="40" className="stroke-slate-100 dark:stroke-slate-800 fill-none" strokeWidth="6" />
                <motion.circle 
                  cx="48" 
                  cy="48" 
                  r="40" 
                  className={`${ring.color} fill-none`} 
                  strokeWidth="6" 
                  strokeDasharray="251.2"
                  initial={{ strokeDashoffset: 251.2 }}
                  animate={{ strokeDashoffset: 251.2 * (1 - ring.percent / 100) }}
                  transition={{ duration: 1.2, delay: idx * 0.1, ease: "easeOut" }}
                />
              </svg>
              <div className="absolute flex flex-col items-center">
                <span className="text-base font-black text-slate-850 dark:text-white leading-none">
                  {ring.score}<span className="text-[10px] text-slate-400 font-extrabold">/{ring.max}</span>
                </span>
                <span className="text-[9px] font-black text-slate-400 dark:text-slate-500 mt-1">
                  {ring.percent.toFixed(0)}%
                </span>
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* Section 3: Comparative Recharts Chart & Recommendation Cards */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        
        {/* Comparative Chart */}
        <div className="bg-white dark:bg-slate-850/80 p-6 rounded-3xl border border-slate-200/80 dark:border-slate-750/70 flex flex-col h-full shadow-md shadow-slate-100/20 dark:shadow-none backdrop-blur-md">
          <h4 className="text-sm font-black text-slate-850 dark:text-white mb-4 uppercase tracking-wider">
            📊 Score Benchmarks vs. Averages
          </h4>
          <div className="h-72 w-full flex-1">
            <ResponsiveContainer width="100%" height="100%">
              <BarChart data={comparisonData}>
                <CartesianGrid strokeDasharray="3 3" stroke="#f1f5f9" className="dark:stroke-slate-800" />
                <XAxis dataKey="name" stroke="#94a3b8" fontSize={10} className="font-extrabold uppercase tracking-wider" tickLine={false} />
                <YAxis stroke="#94a3b8" fontSize={10} className="font-extrabold" tickLine={false} axisLine={false} />
                <Tooltip content={<CustomTooltip />} cursor={{ fill: 'rgba(99, 102, 241, 0.04)' }} />
                <Legend wrapperStyle={{ fontSize: 10, fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: '0.1em', paddingTop: 12 }} />
                <Bar dataKey="User" fill="#6366f1" radius={[6, 6, 0, 0]} name="Your Intensity" />
                <Bar dataKey="Average" fill="#94a3b8" radius={[6, 6, 0, 0]} name="National Avg" />
              </BarChart>
            </ResponsiveContainer>
          </div>
        </div>

        {/* Clinical Recommendations Cards */}
        <div className="bg-white dark:bg-slate-850/80 p-6 rounded-3xl border border-slate-200/80 dark:border-slate-750/70 flex flex-col h-full shadow-md shadow-slate-100/20 dark:shadow-none backdrop-blur-md">
          <h4 className="text-sm font-black text-slate-850 dark:text-white mb-4 uppercase tracking-wider">
            💡 AI Coping Remedies & Self-Care
          </h4>
          
          <div className="space-y-3 flex-1 overflow-y-auto max-h-72 pr-1">
            {result.recommendations.map((rec: string, index: number) => (
              <div 
                key={index} 
                className="flex items-start space-x-3 bg-slate-50/50 dark:bg-slate-900/40 p-4 rounded-2xl border border-slate-200/50 dark:border-slate-800 font-bold text-xs text-slate-700 dark:text-slate-400 hover:border-slate-300 dark:hover:border-slate-700 transition-colors duration-250 shadow-sm"
              >
                <div className="flex-shrink-0 bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 p-1 rounded-lg">
                  <FiCheckCircle className="w-4 h-4" />
                </div>
                <p className="leading-relaxed text-slate-600 dark:text-slate-300">{rec}</p>
              </div>
            ))}
          </div>
        </div>

      </div>

      {/* Custom Share Modal (Framer Motion replacing MUI Dialog) */}
      <AnimatePresence>
        {shareOpen && (
          <div className="fixed inset-0 z-50 flex items-center justify-center p-4">
            <motion.div
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              exit={{ opacity: 0 }}
              onClick={() => setShareOpen(false)}
              className="absolute inset-0 bg-slate-950/45 backdrop-blur-sm"
            />
            <motion.div
              initial={{ scale: 0.95, y: 12, opacity: 0 }}
              animate={{ scale: 1, y: 0, opacity: 1 }}
              exit={{ scale: 0.95, y: 12, opacity: 0 }}
              transition={{ type: 'spring', damping: 25, stiffness: 300 }}
              className="bg-white dark:bg-slate-850 border border-slate-200 dark:border-slate-750/80 rounded-3xl p-7 shadow-2xl max-w-sm w-full relative z-10 space-y-5 text-left backdrop-blur-md"
            >
              <h3 className="text-lg font-black text-slate-900 dark:text-white">Share Diagnostic Report</h3>
              <p className="text-xs text-slate-500 dark:text-slate-400 leading-relaxed font-medium">
                Copy the anonymized link below to share your mental wellness scorecard securely. No demographic identifiers will be exposed.
              </p>
              
              <div className="flex items-center space-x-2 bg-slate-50/60 dark:bg-slate-900/40 p-2 rounded-xl border border-slate-200 dark:border-slate-750">
                <input 
                  readOnly 
                  type="text" 
                  value="http://127.0.0.1:3000/share/report_anonymized_842a" 
                  className="bg-transparent border-none text-xs text-slate-600 dark:text-slate-300 outline-none flex-grow font-bold select-all truncate pl-2"
                />
                <button
                  onClick={handleCopyLink}
                  className="p-2 bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg text-indigo-600 dark:text-indigo-400 hover:bg-slate-50"
                  title="Copy link"
                >
                  <FiCopy className="w-4 h-4" />
                </button>
              </div>

              <div className="flex justify-center gap-3 pt-1">
                <button 
                  type="button"
                  className="w-full py-2 px-4 rounded-xl border border-slate-200 dark:border-slate-700 font-bold text-xs flex items-center justify-center space-x-1.5 hover:bg-slate-50 dark:hover:bg-slate-800 dark:text-white"
                >
                  <FiTwitter className="w-4 h-4 text-sky-500" />
                  <span>Twitter</span>
                </button>
                <button 
                  type="button"
                  className="w-full py-2 px-4 rounded-xl border border-slate-200 dark:border-slate-700 font-bold text-xs flex items-center justify-center space-x-1.5 hover:bg-slate-50 dark:hover:bg-slate-800 dark:text-white"
                >
                  <FiMail className="w-4 h-4 text-slate-400" />
                  <span>Email</span>
                </button>
              </div>
              
              <div className="flex justify-end pt-2 border-t border-slate-100 dark:border-slate-750">
                <button
                  onClick={() => setShareOpen(false)}
                  className="px-4 py-2 bg-slate-100 hover:bg-slate-200 dark:bg-slate-800 dark:hover:bg-slate-750 rounded-lg text-xs font-bold text-slate-700 dark:text-slate-300"
                >
                  Close
                </button>
              </div>
            </motion.div>
          </div>
        )}
      </AnimatePresence>

      {/* Floating Toast Notification */}
      <AnimatePresence>
        {toastMessage && (
          <motion.div
            initial={{ opacity: 0, y: 15 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: 15 }}
            className="fixed bottom-6 right-6 z-50 px-4.5 py-3.5 bg-indigo-600 text-white font-bold rounded-2xl text-xs shadow-lg flex items-center space-x-2"
          >
            <span>✨</span>
            <span>{toastMessage}</span>
          </motion.div>
        )}
      </AnimatePresence>

      {/* Final Reset Button */}
      <div className="flex justify-center border-t border-slate-200 dark:border-slate-750 pt-6">
        <button
          onClick={onReset}
          className="w-full sm:w-96 rounded-2xl py-3.5 bg-indigo-600 hover:bg-indigo-700 text-white font-black text-sm tracking-wide shadow-lg shadow-indigo-200 dark:shadow-none flex items-center justify-center space-x-2.5 transition-all duration-250"
        >
          <FiHeart className="w-4.5 h-4.5 animate-pulse" />
          <span>Reset and Run New Assessment</span>
        </button>
      </div>
    </motion.div>
  );
};

export default PredictionResults;
